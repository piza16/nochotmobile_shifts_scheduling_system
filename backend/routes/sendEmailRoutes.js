// import express from "express";
// import { protect, admin } from "../middleware/authMiddleware.js";

// const router = express.Router();

// router.post("/", protect);
// router.post("/from-admin", protect, admin);

// router.post("/", (req, res) => {});

// export default router;
